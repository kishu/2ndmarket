import { GoodsPurchasedPipe } from './goods-purchased.pipe';

describe('GoodsPurchasedPipe', () => {
  it('create an instance', () => {
    const pipe = new GoodsPurchasedPipe();
    expect(pipe).toBeTruthy();
  });
});
