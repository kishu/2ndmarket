import { GoodsConditionPipe } from './goods-condition.pipe';

describe('GoodsConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new GoodsConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
